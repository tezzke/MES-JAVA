<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.mes</groupId>
        <artifactId>mes-parent</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>mes-acquisition</artifactId>
    <name>mes-acquisition</name>
    <description>① 数据采集层:ModbusTCP 采集 / 模拟数据源 / 扫码枪接入 / 快照队列。</description>

    <dependencies>
        <dependency>
            <groupId>com.mes</groupId>
            <artifactId>mes-core</artifactId>
        </dependency>
        <!-- 仅用于依赖注入注解;不引入任何 Web 框架,ModbusTCP 报文自行实现,无第三方协议库 -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
